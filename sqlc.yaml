version: "2"

sql:
  - engine: postgresql
    queries: sql/queries/*
    schema: sql/migrations/*

    gen:
      go:
        package: db
        out: internal/db
        emit_interface: true
        emit_json_tags: true
        emit_prepared_queries: true
        json_tags_case_style: snake
        query_parameter_limit: 3
